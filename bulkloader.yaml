# Autogenerated bulkloader.yaml file.
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Ad
  connector: csv
  connector_options:
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: billboards
      external_name: billboards
      # Type: Key Stats: 7 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: created
      external_name: created
      # Type: Date/Time Stats: 6 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: email
      external_name: email
      # Type: Email Stats: 6 properties of this type in this kind.
      import_transform: db.Email

    - property: image
      external_name: image
      # Type: Blob Stats: 6 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: image_medium
      external_name: image_medium
      # Type: Blob Stats: 6 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: image_small
      external_name: image_small
      # Type: Blob Stats: 6 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: image_thumb
      external_name: image_thumb
      # Type: Blob Stats: 6 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: phone
      external_name: phone
      # Type: PhoneNumber Stats: 6 properties of this type in this kind.
      import_transform: db.PhoneNumber

    - property: price
      external_name: price
      # Type: String Stats: 6 properties of this type in this kind.

    - property: title
      external_name: title
      # Type: String Stats: 6 properties of this type in this kind.


- kind: Billboard
  connector: csv
  connector_options:
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: ads
      external_name: ads
      # Type: Key Stats: 7 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: created
      external_name: created
      # Type: Date/Time Stats: 4 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: email
      external_name: email
      # Type: Email Stats: 4 properties of this type in this kind.
      import_transform: db.Email

    - property: image
      external_name: image
      # Type: Blob Stats: 4 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: image_medium
      external_name: image_medium
      # Type: Blob Stats: 4 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: image_small
      external_name: image_small
      # Type: Blob Stats: 4 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: image_thumb
      external_name: image_thumb
      # Type: Blob Stats: 4 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: phone
      external_name: phone
      # Type: PhoneNumber Stats: 4 properties of this type in this kind.
      import_transform: db.PhoneNumber

    - property: title
      external_name: title
      # Type: String Stats: 4 properties of this type in this kind.

